import React from "react";
import { Card, Avatar, Skeleton, Switch } from "antd";

const RadioCard = () => {
  const { Meta } = Card;
  return (
    <div>
      <Card></Card>
    </div>
  );
};

export default RadioCard;
